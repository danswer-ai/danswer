<source>
  @type tail
  path /var/log/*.log
  pos_file /var/log/fluentd/tail.pos
  tag ${hostname}
  <parse>
    @type none
  </parse>
</source>

<match **>
  @type forward
  <server>
    host centralized-logger
    port 24224
  </server>
</match>