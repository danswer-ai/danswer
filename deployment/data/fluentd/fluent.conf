<source>
  @type tail
  path /var/log/*.log
  tag "#{Socket.gethostname}"
  <parse>
    @type none
  </parse>
</source>

<match **>
  @type forward
  <server>
    host logs_store
    port 24224
  </server>
</match>